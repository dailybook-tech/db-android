<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- UPI Intent flow (targetSdk 30+): allow querying apps that handle UPI and specific UPI packages.
         Intent lets queryIntentActivities() see UPI handlers; explicit packages ensure getPackageInfo()
         works when intent resolution is empty (e.g. some OEMs) so installed UPI apps stay selectable. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <package android:name="com.phonepe.app" />
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.google.android.apps.navi.market.activity" />
        <package android:name="com.google.android.apps.walletnfcrel" />
        <package android:name="net.one97.paytm" />
        <package android:name="in.org.npci.upiapp" />
        <package android:name="in.amazon.mShop.android.shopping" />
        <package android:name="com.freecharge.android" />
        <package android:name="com.mobikwik_new" />
        <package android:name="com.myairtelapp" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application>
        <activity
            android:name=".screen.BookKeepActivity"
            android:theme="@style/Theme.Laborbook"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing"/>

        <activity
            android:name=".screen.AppLockActivity"
            android:theme="@style/Theme.Laborbook"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:noHistory="true"/>

        <!-- Razorpay CheckoutActivity with custom theme for edge-to-edge -->
        <activity
            android:name="com.razorpay.CheckoutActivity"
            android:theme="@style/RazorpayCheckoutTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            tools:node="merge"
            tools:replace="android:theme"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.laborbook.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>